name:                fantasypie
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5
- containers
- trifecta
- HUnit
- prettyprinter

library:
        source-dirs: src

executables:
        pie:
                main: Main.hs
                source-dirs: app/
                dependencies:
                        - fantasypie
tests:
  pie-test:
    main:                Discovery.hs
    source-dirs:         test
    dependencies:
        - tasty
        - tasty-hunit
        - fantasypie
